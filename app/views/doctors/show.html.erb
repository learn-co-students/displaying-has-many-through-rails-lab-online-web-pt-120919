#show all dr appts ina human readable format
#step 1 iterte of @doctor.appointments

<% @doctor.appointments.each do |appt| %> 
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> 
    <%= link_to appt.patient.name, patient_path(appt.patient) %>
    <%end%>
    #link to the patients show page
  